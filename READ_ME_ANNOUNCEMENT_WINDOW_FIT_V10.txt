SFK ClassBoard - Announcement Window Fit Fix v10

Fix:
- Full screen still uses large auto-fit announcement text.
- Smaller/non-fullscreen desktop windows no longer shrink announcement text too much.
- Header, subject, published date, teacher, buttons, and heart remain protected.
- If a post is too long for the shorter window, only the announcement text area scrolls.
- No forced bold; bold only appears when the Bold tool is used.

Upload after extracting the ZIP. If the old layout remains, hard refresh or open reset-cache.html once.
