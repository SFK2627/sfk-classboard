SFK ClassBoard - Emergency Restore v15

This package restores the correct index.html.

Your live GitHub page showed raw CSS text. That means the repo's root index.html was replaced with CSS content or the wrong file was uploaded into index.html.

Use the FULL deploy-safe ZIP:
1. Extract the ZIP.
2. Open the extracted folder.
3. Upload the CONTENTS directly to the GitHub repo root.
4. Confirm GitHub root shows index.html as an HTML file, not CSS text.
5. Commit changes.
6. Open reset-cache.html once, then reload the site.

Do not upload only class-chat.css as index.html.
Do not rename class-chat.css to index.html.
