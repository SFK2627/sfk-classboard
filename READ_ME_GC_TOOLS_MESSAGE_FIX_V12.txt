SFK ClassBoard - GC Tools + Deleted Message Label Fix v12

Fixes:
1. Adds GC Admin Tools inside the GC More Tools menu for Adviser/Admin.
2. Admin can add GC members inside the GC using Student ID | Full Name.
3. Admin can refresh/enable/disable members inside the GC.
4. Admin can delete the entire GC conversation inside the GC.
5. Student-deleted messages no longer say "removed by the Adviser."
   - If a student deletes their own message, it says "Message removed by the sender."
   - If the adviser removes it, it says "Message removed by the Adviser."

After upload:
- Hard refresh.
- If old files still show, open reset-cache.html once.
